version: "3.7"

services:
  flask:
    #container_name: fltest_web
    build: .
    ports:
      - 5000:5000
    volumes:
      - ./src:/code
    tty: true
    environment:
      TZ: Asia/Tokyo
    command: flask run --host 0.0.0.0 --port 5000